import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'basic-todo-app',
  appName: 'app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
